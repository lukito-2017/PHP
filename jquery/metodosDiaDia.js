/**
 * $.trim - retira os espaços antes e depois do texto. $.trim(valor);
 * $.type - retorna o tipo de varialvel - $.type(idade) o typeof faz a mesma coisa
 * $.isNumeric - retorna verdadeiro se for número
 *
 * $('#campo').attr('disabled', 'disabled');  - desabilita um elemento html
 * $('#campo').removeAttr('disabled');  - habilita um elemento html
 * $('#Idade').val() - exemplo de como pegar um valor de um selected
 */